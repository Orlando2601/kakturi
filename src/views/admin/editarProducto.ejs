<!DOCTYPE html>
<html lang="en">
    <%- include ('../partials/head') %>
<body>
    <%- include ('../partials/header') %> <!-- importando header -->
    <main class="mainCrearProducto">
        <section class="imagen-edit">
            <img src="/images/dbProductos/<%= toEdit.imagen %>" alt="">
        </section>
        <h1>Complete los datos del nuevo producto</h1>
        <form action="/admin/editar/<%= toEdit.id %>?_method=patch" method="POST" enctype="multipart/form-data" > <!-- enctype="multipart/form-data"> --> <!-- Usar esta config cuando se va a subir imagenes en el form sino se puede quitar el elemento o configurar  enctype="application/x-www-form-urlencoded"  para solo texto -->            
            <div>
                <label for="titulo">Nombre</label>
                <input type="text" id="titulo" name="titulo"  value="<%= toEdit.titulo %>"  >
            </div>
            <div>
                <label for="descripcion">Descripción</label>
                <input type="text" id="descripcion" name="descripcion"  value="<%= toEdit.descripcion %>"  >
            </div>
            <div>
                <label for="precio">Precio</label>
                <input type="text" id="precio" name="precio" value="<%= toEdit.precio %>"  >
            </div>

            <div>
                <label for="colores">Colores</label>
                 <input type="text" id="colores" name="colores" value="<%= toEdit.colores.join(', ') %>" >
            </div>
            <div>
                <label for="imagen">Imagen de descripción</label>
                <input  type="file" id="imagen" name="imagen" placeholder="Ingrese imagen de referencia" >
            </div><!-- caja de carga de archivos -->
            <div id="guardar">
                <input id="crearProducto" type="submit" value="Guardar">
            </div> <!-- caja botones -->
        </form><!-- formulario de edicion de productos -->
    </main>
    <%- include ('../partials/footer') %> <!-- importando footer -->
    </body>
</html>