<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <link rel="stylesheet" href="/css/styles.css"> <!-- Configuración de carpeta para diseños CSS -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet" /><!-- Link para obtener iconos de la pagina enlazada, para agregar se debe ver la sintaxis que muetsra la pagina -->
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
</head>
<body>
    <%- include ('../partials/header') %><!-- header importado -->
    <main class="registro">
        <div class="iconoUser">
            <i class="far fa-user"></i>
        </div><!-- icono usuario-->

        <form action="/user/registro" method="POST">

            <label for="nombre">Ingrese nombre</label>
            <input type="text" name="nombre" id="nombre"  value="<%= locals.old && old.nombre ? old.nombre : '' %>">
            <% if (locals.errors){let errorname = errors.find(error => error.param == 'nombre')%>
                <p class="showError"><%= errorname.msg%></p>
    
            <%  } %> <!-- Este codigo me permite mostrar los errores deprueba  en la vista -->
            <label for="apellido">Ingrese apellido</label>
            <input type="text" name="apellido" id="apellido" value="<%= locals.old && old.apellido ? old.apellido : '' %>">
            <% if (locals.errors){let errorname = errors.find(error => error.param == 'apellido')%>
                <p class="showError"><%= errorname.msg%></p>
    
            <%  } %> <!-- Este codigo me permite mostrar los errores deprueba  en la vista -->
            <label for="correo">Ingrese Correo electrónico</label>
            <input type="email" name="correo" id="correo" value="<%= locals.old && old.correo ? old.correo : ' ' %>">
            <% if (locals.errors){let errorname = errors.find(error => error.param == 'correo')%>
                <p class="showError"><%= errorname.msg%></p>
    
            <%  } %> <!-- Este codigo me permite mostrar los errores deprueba  en la vista -->
            <label for="contraseña">Ingrese Contraseña</label>
            <input type="password" name="contraseña" id="contraseña" >
            <% if (locals.errors){let errorname = errors.find(error => error.param == 'contraseña')%>
                <p class="showError"><%= errorname.msg%></p>
    
            <%  } %> <!-- Este codigo me permite mostrar los errores deprueba  en la vista -->
            <div class="botones">
                <input class="btn" type="submit" value="Registrar">
                <input class="btn" type="reset" value="Borrar">
            </div><!-- caja botones formulario -->

        </form><!-- formulario de carga de datos de registro -->

        
    </main>
    <%- include ('../partials/footer') %> <!-- footer importado -->
    </body>
</html>